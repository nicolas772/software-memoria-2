import React, { useState } from 'react';

export default function DashboardGeneralIteration(props) {
    const { content } = props;
    return (
        <div>
          Dashboard General Iteration
        </div>
      );
}